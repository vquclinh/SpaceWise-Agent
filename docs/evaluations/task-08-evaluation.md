## Task 8: Requirement Change Analysis
Overall score: 4.9 / 5

| Criterion | Weight | Score | Notes |
|---|---|---|---|
| 1. Maintenance impact level change | high (~20%) | 5 | Two-level system (OutOfService blocks / Advisory acknowledges) fully identified with all §1.1 sub-rules: multiple concurrent records at different levels (§4.2), escalation/downgrade while open (§4.3), and escalation-to-OOS making overlapping approved bookings identifiable to staff (`booking_alerts` + `TR_maintenance_escalation`). Status-vs-Impact reconciliation explicitly decided — blocking queries `maintenance_records` directly, `spaces.current_status` demoted to UI-only (§4.1). |
| 2. Affected entities and attributes | high (~17.5%) | 5 | `maintenance_records` gains `impact_level` (history/audit-trail interpretation via `maintenance_impact_history`, justified). Advisory acknowledgement correctly recognized as a **new structural junction** (`booking_advisory_acknowledgments`, `UNIQUE (booking_id, maintenance_id)`), not a boolean flag. Booking linkage covered. Extra entities (facility_assets, auto_approval_policies, user_roles) are all tagged [EXTENSION] / documented amendments with explicit rationale — change surface not over-extended without justification. |
| 3. Affected relationships | medium (~6.7%) | 5 | New M:N Booking–acknowledges–MaintenanceRecord resolved via junction; modified booking-space-maintenance constraint logic (OOS-only) noted as logic change, not just attribute; escalation reverse-lookup need explicitly called out (§3). |
| 4. Concurrency conflict identification | high (~20%) | 5 | Names the conflict type precisely (check-then-act / lost-update / TOCTOU, §5.1) and describes interleavings for double-approval (§5.2) and manual-vs-auto / instant-vs-instant (§5.3). **Both** booking paths identified as conflict sources. Solution is specific and implementable: `SERIALIZABLE` + `WITH (UPDLOCK, HOLDLOCK)` or `sp_getapplock`, with filtered index `IX_bookings_space_status_time` and mandatory retry on 1205/1222. Bonus: lock-ordering rule `bookings → maintenance_records → booking_alerts` for the approval-vs-escalation deadlock. |
| 5. Business rules updated | high (~17.5%) | 5 | Replaced vs. preserved vs. new clearly distinguished (§1.2 unchanged list, §1.3 Phase 1→Phase 2 table, §4.x analyses, 22-row traceability matrix). Phase 1 "space under maintenance cannot be booked" explicitly retired and re-scoped to OutOfService (§4.1, §4.2). New rules listed with [CONFIRMED]/[EXTENSION] tags. |
| 6. Reporting needs analysis | medium (~6.7%) | 5 | All four §1.3 reports enumerated (§1.1, matrix rows 15–18) with data requirements: utilization interval via COALESCE fallback (§4.4), weekday/hour extraction noted, room finder needs Space + facility + OOS/conflict exclusion (§4.6, row 17), escalation lookup needs `impact_level` + `space_id` + overlap (rows 18). Indexing candidates identified (`space_id`, `status`, `requested_start_time`, `requested_end_time` on `bookings`). |
| 7. Traceability to Phase 1 | medium (~6.7%) | 5 | Every change references the specific Phase 1 artifact it touches: §1.6 table maps outputs 04–07 to consuming tasks; §2.2 cites the exact Phase 1 DDL line (`outputs/05-db-definition-G08.sql:103`, verified correct for the unnamed `quantity` default); the Phase 1 trigger `TR_bookings_PreventOverlapAndUnavailable` and its exact predicate are quoted and its required modifications specified (B8). Task 09 can be opened directly from this document. |
| 8. Scope discipline | low (~5%) | 3 | A strong analysis, but it crosses into design territory the rubric assigns to tasks 09–12: full column-level specs for 8 new tables, named triggers/constraints/filtered index, and executable SQL blocks (§4.1, §4.2, §5.4). Largely justified by the shared skill's mandatory granular-mapping requirement and the AGENTS.md documented amendments, but as a pure "requirement change analysis" it goes further than strictly necessary. Also minor internal tension: §1.6 claims "no DDL" while SQL appears in the body. |

### Strengths:
- **Concurrency analysis is the standout** — both paths, named isolation + lock hints, interleaving diagrams, deadlock-vs-timeout error numbers (1205/1222), and mandatory lock-ordering discipline. This is the highest-value section and it is complete.
- **Status-vs-Impact reconciliation decided, not deferred** — the Phase 1 `current_status` blocking role is explicitly retired and replaced with a direct `maintenance_records` query, with the trigger correctly slated for modification rather than left as a stale backstop (B8).
- **Acknowledgement treated as structure, not a flag** — `booking_advisory_acknowledgments` preserves *which* advisories were acknowledged, and the count-vs-containment bug is explicitly caught and fixed (L2).
- **Exceptionally granular traceability** — 22-row matrix maps every requirement to concrete artifacts; references to Phase 1 outputs were verified accurate against the actual files.
- **Honest scoping** — [EXTENSION] disclosures, the refuted-FD proof (no XOR needed on `space_id`/`asset_id`), the rejected two-table split, and documented table drops (§1a–§1c) are all stated with trade-offs rather than hidden.
- **Inherited-issue discipline** — claims about Phase 1 (unnamed quantity default, trigger predicate, role column) match the actual Phase 1 DDL; stale-migration warning (S3) is accurate — `10-schema-migration-G08.sql` exists and predates the amendments.

### Issues found:
- [minor] Scope discipline: the document proposes full column specs, named constraints/triggers/indexes, and SQL blocks that the rubric assigns to tasks 09–12. Justified by the skill's granular-mapping mandate, but flagged per rubric.
- [minor] Clarity: heavy forward-references to Output 09 and the internal Appendix A ID scheme (D/L/R/S/T/B) mean a reader must keep Output 09 open to fully follow some claims; the document is not fully self-contained in places.
- [minor] Internal-tension wording: §1.6 states "this document is analysis — no DDL" while §4.1/§4.2/§5.4 contain executable T-SQL.

### Inherited issues from earlier tasks (if any):
- None. Phase 1 baseline facts relied on by this output (the trigger predicate `current_status IN ('UnderMaintenance','TemporarilyClosed','Retired')`, `quantity INT NOT NULL DEFAULT 1` unnamed at `05:103`, `user_accounts.role`, `space_facilities.quantity`) were verified against `outputs/05-db-definition-G08.sql` and match.

### Suggested fixes:
1. (Optional, low priority) Move the executable SQL blocks' rationale into prose and leave full DDL/pseudocode to Task 09, or add an explicit note that they are analysis-level logic illustrations — reconciling §1.6's "no DDL" claim with the body.
2. (Optional) Add a one-sentence self-containment note for the L/D/R/T/B IDs or a short glossary mapping them to sections, so the document reads standalone.
3. No substantive content changes required before Task 09 consumes this output.
